# Политика двуязычной синхронизации (справка)

> Перевод для человека. **Enforce:** [`.cursor/rules/cursor-bilingual-sync.RULE.md`](../../.cursor/rules/cursor-bilingual-sync.RULE.md).

- Источник для агента — только **английский** [`.cursor/`](../../.cursor/).
- Русское зеркало — **`docs/cursor-ru/`**, только для чтения.
- В зеркале: обычные `rules/*.md`, **без** `*.RULE.md` и **без** `alwaysApply`.
- Не вкладывать русские переводы и зеркало в `.cursor/`.
- При правках `.cursor/` — обновлять соответствующий путь здесь (`commands/sync-cursor-bilingual-structure` в `.cursor/commands/`).
